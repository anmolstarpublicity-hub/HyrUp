# ── Admin PC — central_api.py ────────────────────────────────
# Run: pip install -r requirements.txt

flask>=3.0.0
flask-socketio>=5.3.0
flask-limiter>=3.5.0
flask-cors>=4.0.0
eventlet>=0.35.0
supabase>=2.0.0
requests>=2.31.0
werkzeug>=3.0.0
pyinstaller>=6.0.0
