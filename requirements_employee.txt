# ── Employee PC — collector.py ───────────────────────────────
# Bundled into HyrUpCollector.exe via PyInstaller — no manual install needed
# Only needed if running collector.py directly with Python

pygetwindow>=0.0.9
pynput>=1.7.6
Pillow>=10.0.0
supabase>=2.0.0
python-socketio[client]>=5.10.0
python-dotenv>=1.0.0
